import React from 'react'
import CenteredButton from '../Base/CenteredButton'

export default function FrontPageMainLogin() {
    return (
        <CenteredButton buttonText={'Login'} navlink={'/login'} />
    )
}
